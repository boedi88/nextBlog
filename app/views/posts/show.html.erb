<p>
  <h3><%= @post.title %></h3>
</p>

<p>
  <%= sanitize @post.content %>
</p>

<%if  !current_user.nil? && current_user.role%>
  <%= link_to 'Edit', edit_post_path(@post) %> |
  <%= link_to 'Back', posts_path %>
<%end%>
  
<h4>Comments</h4>
<div class="comments">
<%unless current_user%>
  You need to be logged in to view and add comments.<br/>
  Please <%= link_to "log in", new_user_session_path%> first.
<%else%>
Show comments.
<%end%>
</div>